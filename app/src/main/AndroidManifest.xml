<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appclinica">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.autohipnosis.AutohipnosisActivity"></activity>
        <activity android:name=".ui.exercise.MultimediaActivity" />
        <activity android:name=".ui.exercise.PasosActivity" />
        <activity android:name=".ui.psicologo.DisplayPsicoActivity" />
        <activity android:name=".ui.chat.SalaDeChatActivity" />
        <activity android:name=".ui.chat.ChatActivity" />
        <activity android:name=".ui.chatnofunciona.ChatTest" />
        <activity android:name=".ui.psicologo.PsicoActivity" />
        <activity android:name=".ui.chatnofunciona.SalaChatActivity" />
        <activity android:name=".ui.exercise.EjercicioActivity" />
        <activity
            android:name=".ui.exercise.ExerciseActivity"
            android:label="@string/title_activity_exercise"
            android:theme="@style/NoActionBar" />
        <activity android:name=".ui.chatnofunciona.ChatActivity" />
        <activity android:name=".onboarding.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.PrincipalActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"
            tools:replace="android:value" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>